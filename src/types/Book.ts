import BookCard from "../components/BookList/BookCard/BookCard"

type Book = {
    _id: string;
    title: string;
    coverage: string;
    author: string;
    year: number;
    description: string;
}

export default Book;