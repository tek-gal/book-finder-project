import styled from 'styled-components';


export type BookCardProps = {
  // title: string;
  // author: string;
  // description: string;
  // year: string;
};

export const BookCardStyles = styled.div<BookCardProps>``;
